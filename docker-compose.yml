name: graphify-swarm

services:
  qdrant:
    image: qdrant/qdrant:v1.13.6
    container_name: graphify-qdrant
    ports:
      - "6333:6333"   # REST API
      - "6334:6334"   # gRPC
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_storage:
